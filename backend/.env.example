DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=lms_db

SECRET_KEY=change_this_to_a_long_random_string_at_least_32_chars

# Leave SMTP_USER and SMTP_PASSWORD empty to skip email sending (no crash)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
